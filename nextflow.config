nextflow.enable.dsl=2

params {
    samplesheet = "data/samplesheet.csv"
    ref         = "data/reference.fasta"
    outdir      = "results"
}

process {
    executor = 'local'
    container = 'ngs-pipeline:1.0'
}

profiles {
    docker {
        docker.enabled = true
    }
}
